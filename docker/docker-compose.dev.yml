services:
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: pricestore-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
